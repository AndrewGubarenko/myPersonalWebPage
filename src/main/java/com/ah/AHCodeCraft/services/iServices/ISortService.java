package com.ah.AHCodeCraft.services.iServices;

public interface ISortService {
    Long mergeSort(int arraySize);

    Long bubbleSort(int arraySize);

    Long insertionSort(int arraySize);

    Long selectionSort(int arraySize);

    Long heapSort(int arraySize);
}
